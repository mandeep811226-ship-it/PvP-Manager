<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Required for Android 13+ notification permission runtime request -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required to prompt user to disable battery optimisation for this app -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".PvpService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!--
            FileProvider — required for exportBlsMemory() in AndroidBridge.java.
            FileProvider.getUriForFile() looks up this <provider> block at runtime
            to resolve the authority "com.pvpmanager.fileprovider".
            If this block is absent the call throws:
              "Couldn't find meta-data for provider with authority: …fileprovider"
            which is the exact error the user reported.

            android:authorities uses ${applicationId} so it automatically matches
            the package name in any build variant (debug / release / renamed pkg).

            The paths resource (@xml/file_paths) grants read URI permission over
            the app's internal cache dir (getCacheDir) and, when available, the
            external cache dir (getExternalCacheDir).  Both are tried by
            exportBlsMemory() with external-cache as the preferred location.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>
